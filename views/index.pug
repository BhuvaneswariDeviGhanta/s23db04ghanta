extends layout

block content
  h1= title
  p Welcome to Render page
  <button type="button" class="btn btn-danger" onclick="goFaster()">Press Me</button>
  br 
  br

  div(id="faster") Increase the speed
  br
  script.
      var mph=20;
      var change=1;
      function goFaster() {
        mph =mph + change ;
        change =change +1;
        document.getElementById("faster").innerHTML=`MPH ${mph} with change ${change}.`;
      }